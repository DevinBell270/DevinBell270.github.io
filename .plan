# Plan
- Check if a Jekyll serve command is already running; stop it if so.
- Start `bundle exec jekyll serve --livereload --host 0.0.0.0 --port 4000` from `/Users/devin.bell/Documents/GitHub/DevinBell270.github.io`.
